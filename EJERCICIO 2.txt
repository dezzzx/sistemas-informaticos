
EJERCICIO 1:
$ echo capitulo1.txt

$ echo "Git es un sistema de control de versiones ideado por Linus Torvalds" > capitulo1.txt

$ git add capitulo1.txt

$ git status

$ git commit -m "Mensaje del commit"

$ git log
EJERCICIO 2:

$ echo "El flujo de trabajo basico con Git consiste en : 1-Hacer cambios en el repositorio.2-Añadir los cambios a la zona de intercambio temporal. 3-Hacer un commit de los cambios." > capitiulo2.txt

$ git add capitiulo2.txt

$ git status

$ git commit -m "Añadido capitulo 2"
EJERCICIO 3:

$ echo "Git permite la creacion de ramas lo que permite tenre distinas versiones del mismo proyecto y trbajar de manera simultanea en ellas " > capitiulo3.txt

$ git add capitiulo3.txt

$ git commit -m "Añadido capitulo 3"

$ git status

EJERCICIO 4:


$ echo "Capitulo 5 : Conceptos avanzados " > indice.txt

$ git add indice.txt

$ git commit -m "Añadido al final del indice el capitulo 5"

$ git status




